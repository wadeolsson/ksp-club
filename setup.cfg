[metadata]
name = ksp-club
version = 0.1.0
description = KSP CLUB — Dual-State Merge System

[options]
packages = find:
python_requires = >=3.9

[options.entry_points]
console_scripts =
    ksp-club = merger.cli:main
